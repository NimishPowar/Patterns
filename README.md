Star patterns 

Outer loop for number of lines
Inner loop for what needs to print

Number patterns

same as star pattern just add a new variable num and incement it with each iteration of either outer or inner loop

